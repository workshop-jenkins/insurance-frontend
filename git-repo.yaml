apiVersion: environment.cloudbees.com/v1alpha1
kind: GitRepository
metadata:
  name: workshop-jenkins-insurance-frontend                             
  namespace: previews
spec:
  url: https://github.com/workshop-jenkins/insurance-frontend.git
  apiTokenSecretRef:
    name: beedemo-dev-github-token
  autoUpdateContexts:                           
  - default
